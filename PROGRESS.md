# PLEXO — Estado del proyecto

Última actualización: 2026-07-23. Repo: `github.com/albionsistemas/plexo`, rama `main`.

## Resumen ejecutivo

Los 9 módulos de negocio del brief original están construidos y commiteados. Hoy, por primera vez, se corrió todo contra una base de datos real (hasta ahora solo compilaba/testeaba con mocks) — se encontró y arregló un bug real, y se confirmó que el aislamiento por tenant (RLS) funciona de verdad, no solo en el papel.

## Módulos completos (orden de commits en `main`)

| Commit | Qué es |
|---|---|
| `af5726c` | Scaffold del monorepo Nx (api NestJS+Fastify, web Next.js, Prisma+RLS) |
| `330d664` | Contexto de tenant (transacción por request) + guards RBAC |
| `8de24c5` | Login, JWT, `JwtAuthGuard` |
| `accae3c` | Logo/branding |
| `a205e83` | **Inventario** — artículos/variantes, stock por depósito, auditoría vía triggers |
| `afbc443` | **Facturación** — multi-moneda, descuentos, notas de crédito, bloqueo fiscal tras CAE |
| `0861c95` | **Cuentas a Cobrar** — antigüedad de saldos, deuda por cliente |
| `f81a305` | **Contabilidad** — partida doble, balance de sumas y saldos, asientos inmutables |
| `2ce6502` | **Impuestos** — tasas versionadas, delegables al contador |
| `ce29eee` | **Reportes** (Resultados, Ventas, Financiero) |
| `7c0c952` | Fix: `DatabaseModule` necesitaba ser `@Global()` — encontrado recién al levantar contra una base real |

## Decisiones de arquitectura (para no repetir el porqué después)

- **RLS multitenancy**: cada tabla tiene `tenantId`; las políticas filtran por `current_setting('app.tenant_id')`. **Falla cerrado** — probado hoy: sin `tenant_id` seteado, o con el de otro tenant, no se ve nada.
- **Auditoría + bloqueos fiscales**: triggers de Postgres, no middleware de la app (así no se puede esquivar desde ningún cliente/conexión).
- **Entre módulos**: nunca se importa el `Service` de otro módulo. O se consulta la tabla directo vía `getTenantDb()` (RLS igual protege), o se compone a nivel `apps/api` (ver `SalesService`, que junta Facturación+Inventario).
- **`ModuleAccessGuard` + `UserModuleAccess`**: permisos granulares por módulo, pensado específicamente para el contador externo.
- **Venta sin factura ("asiento manual por ahora")**: `POST /inventory/movements` permite un `SALE_OUT` sin `invoiceId` — stock que sale sin pasar por Facturación (venta informal, ajuste que hace de venta, etc.). Esto NO dispara el auto-posteo contable a propósito: Inventario solo conoce cantidades, nunca precio/moneda, así que no hay con qué generar un asiento correcto ahí. Decisión (2026-07-21): por ahora esos casos requieren que alguien postee el asiento a mano vía `POST /accounting/journal-entries` (ya existe). Si esto resulta ser un flujo frecuente, la alternativa es forzar que toda venta pase por `SalesService`/Facturación (con un "ticket interno" sin CAE real) para tener una sola fuente de verdad — pero eso le agrega fricción a la venta informal, por eso no se hizo todavía.
- **Enumerar tenants para trabajos de sistema (crons)**: nada en la app (rol `plexo_app`) podía listar `tenants` — tiene RLS forzado igual que todo lo demás, y hasta ahora nada lo necesitaba (login siempre recibe el tenantId del formulario). El primer cron (recordatorios de Cuentas a Cobrar) sí necesita recorrer "todos los tenants". Decisión (con el usuario, 2026-07-21): función Postgres `list_tenant_ids()` `SECURITY DEFINER` (migración `20260726000000_list_tenant_ids_function`), dueña del rol admin/migración, con `GRANT EXECUTE` a `plexo_app` — el mismo patrón que `audit_log_capture()` ya usaba. Sólo expone "listar ids", nada más; `plexo_app` sigue sin poder leer `tenants` directo (verificado con psql). Cualquier cron futuro que necesite recorrer tenants reusa esta misma función.
- **Avatar de usuario = solo URL** (decisión con el usuario, 2026-07-21): no hay ninguna infraestructura de almacenamiento de archivos en el proyecto. En vez de sumar upload real (S3/disco), el perfil solo guarda una URL (`User.avatarUrl`); sin ella, el frontend genera un avatar con las iniciales del nombre/email. Revisar esta decisión si en algún momento se necesita upload real.
- **`UserActivityLog` vs `AuditLog`**: son cosas distintas a propósito. `AuditLog` (ya existía) es un log de cambios de fila vía triggers de Postgres — no tiene ni puede tener IP/user-agent (eso no existe a nivel trigger). `UserActivityLog` (nuevo) es a nivel aplicación: quién, cuándo, desde qué IP, qué acción, éxito o fallo. Se llena desde dos lugares: `ActivityLogInterceptor` (`libs/shared/database`, registrado junto a `TenantContextInterceptor`) para todo request mutante (POST/PUT/PATCH/DELETE) autenticado, y directamente en `AuthService.login()` para intentos de login (éxito y fallo) porque el login corre ANTES de que exista cualquier contexto de tenant/usuario que el interceptor pueda enganchar. Deliberadamente no cubre GETs (sería puro ruido) ni acciones de un cron/sistema (esos no tienen un usuario ni un request HTTP detrás). Cada escritura del log corre en su PROPIA transacción (`withTenantContext` separada), no la misma que la acción de negocio — si la acción de negocio falla y su transacción se revierte, el registro de "esto falló" tiene que sobrevivir igual; si compartieran transacción, un fallo real se llevaría puesto también el rastro de que ocurrió.
- **`Company` reemplazó a `Customer`** (decisión con el usuario, 2026-07-21, las tres preguntas se resolvieron con la opción recomendada): una "empresa" puede ser CLIENTE, PROVEEDOR y/o SUCURSAL/punto de venta del propio tenant, con **varios roles a la vez** (tabla `company_roles`, no un enum único — un distribuidor puede ser cliente y proveedor simultáneamente). La migración (`20260728000000_companies_and_people`) hace `ALTER TABLE customers RENAME TO companies` (no drop+recreate — conserva filas/FKs/policies/grants, que en Postgres cuelgan del OID de la tabla, no del nombre) y **migra automáticamente** cada fila existente a `company_roles` con rol `CUSTOMER`, así Facturación/Cuentas a Cobrar/Reportes siguieron andando sin tocar datos. Truco de bajo-diff: el campo de relación en `Invoice`/`Quote` se dejó llamado `customer` (apunta a `Company`, no a `Customer`) — así todo `include: { customer: true }` e `invoice.customer.name` en el código existente siguió andando tal cual; sólo cambiaron las llamadas al delegado de Prisma (`db.customer.x` → `db.company.x`), no las relaciones anidadas.
- **`Person` (contactos) sólo para CUSTOMER/SUPPLIER, nunca para BRANCH**: una sucursal es el tenant mismo, no un tercero con un contacto — `CompaniesService.createPerson` rechaza con 400 si la empresa referenciada no tiene rol CUSTOMER o SUPPLIER (probado).
- **Punto de venta fiscal formalizado vía Company-rol-BRANCH** (decisión con el usuario, 2026-07-21): antes `Invoice.pointOfSale` era un string que el que facturaba tipeaba a mano; ahora `CreateSaleDto` (el único punto de entrada real para crear facturas) pide `branchId` en vez de `pointOfSale`, `SalesService.createSale` resuelve `Company.pointOfSaleNumber` de esa sucursal (rechaza con 400 si la empresa no es BRANCH o no tiene el número configurado) y arma la factura con ese valor — el número de comprobante interno (`nextInvoiceNumber`) sigue agrupando por ese string igual que antes, no cambió esa lógica.
- **Modo claro/oscuro: clase manual en `<html>`, no sólo `prefers-color-scheme`** (decisión con el usuario, 2026-07-22): Tailwind v4 usa `@media (prefers-color-scheme: dark)` para `dark:` por default, pero eso no permite que el usuario elija — se sobreescribió con `@custom-variant dark (&:where(.dark, .dark *));` en `global.css` para que dependa de la clase `.dark` en `<html>`, que `ThemeProvider` togglea. El primer render usa la preferencia del sistema como default (no fuerza oscuro ni claro a ciegas), pero una vez que el usuario elige algo, gana esa elección — persistida en `localStorage`, no por tenant/usuario en la base (es una preferencia de este navegador, no de la cuenta).
- **Migración de paleta dark→light por script, no a mano pantalla por pantalla**: con 32 archivos y ~550 clases de color hardcodeadas, escribir cada `dark:` a mano hubiera sido lento y propenso a inconsistencias. Se usó un mapeo mecánico y reversible (espejo de la escala de grises/colores de Tailwind: `N ↔ (1000-N)` redondeado a los steps reales — 50↔950, 100↔900, etc., con excepciones explícitas para rellenos sólidos tipo botón que ya leían bien en ambos fondos, como `bg-indigo-600`) corrido una sola vez con Node. Cualquier pantalla nueva que se agregue de acá en adelante tiene que escribir el par `light dark:` a mano desde el vamos — el script no es una herramienta que quede corriendo, fue un migrador de una sola pasada.
- **Panel lateral deslizante para detalle de factura, no otro modal centrado** (decisión con el usuario, 2026-07-22): hasta ahora las 13+ modales de la app (`CompanyDetailModal`, `StatementModal`, `ReceiptModal`, etc.) son todas el mismo patrón — overlay centrado, `fixed inset-0 flex items-center justify-center`. Para ver una factura completa (líneas, cobros, notas de crédito) ese patrón no alcanza: son varias secciones y perder de vista la tabla de fondo no suma nada. `InvoiceDetailPanel` es el primer panel lateral de la app: mismo overlay pero `flex justify-end`, panel `h-full max-w-lg` con borde izquierdo en vez de `rounded-xl` en las 4 esquinas, y una transición `translate-x-full → translate-x-0` en el próximo frame tras montar (no había ningún precedente de `transition-transform` para paneles en el código, sólo en hovers). Se cierra al instante (sin animación de salida), igual que el resto de las modales — no vale la pena la complejidad de un unmount demorado por consistencia con algo que en ningún otro lado de la app tiene esa animación tampoco.
- **Empresas: borrado lógico (`active`), no `DELETE`** (decisión con el usuario, 2026-07-22, corrigiendo mi propio approach a mitad de implementación): arranqué construyendo un `DELETE /companies/:id` duro con reglas de bloqueo (rechazar si tenía facturas/presupuestos) — el usuario interrumpió a mitad de la prueba para preguntar si no convenía mejor un booleano "activa/desactivada". Tiene sentido: una `Company` puede estar referenciada por facturas/notas de crédito ya emitidas (sin cascada de borrado a propósito, ver la migración), así que un DELETE real siempre iba a terminar en "no se puede borrar, tiene historial" — la UX real que hace falta es "dejar de operar con esta empresa", no destruir el registro. Se reemplazó por `Company.active` (default `true`); `listCompanies` la filtra por default, `includeInactive=true` la trae de vuelta. **`Person` (contactos) sigue con hard delete real** — a diferencia de `Company`, nada en el schema referencia un `Person` y no tiene significancia fiscal, así que no hay historial que proteger. Todo código nuevo que resuelva una `Company` para crear un registro *hacia adelante* (nueva factura, nueva venta contra una sucursal) tiene que chequear `active` y rechazar si está desactivada; código que mira historia hacia atrás (antigüedad de saldos, reportes de ventas pasadas) NO debe filtrar por `active` — la empresa desactivada sigue habiendo existido cuando se generó ese dato.
- **AFIP padrón (`ws_sr_padron_a13`, búsqueda por CUIT) vs. AFIP WSFE (`wsfe`, factura electrónica/CAE) son dos integraciones distintas** (decisión con el usuario, 2026-07-22, corrigiendo un supuesto equivocado de una nota de diseño anterior): ambas necesitan el mismo mecanismo de autenticación (certificado digital propio + WSAA), pero son autorizaciones de servicio separadas en el Administrador de Relaciones de Clave Fiscal de AFIP — tener el cert andando para una no habilita automáticamente la otra. `libs/modules/companies` implementa el mismo patrón puerto/stub/real que ya usaba `invoicing` para el email (`AfipPadronPort` / `StubAfipPadronService` / `RealAfipPadronService`, wireados por `AFIP_CERT_PATH`/`AFIP_KEY_PATH`/`AFIP_CUIT_REPRESENTADA`/`AFIP_ENV` en `companies.module.ts`, mismo `useFactory` que `createEmailSender()`). La firma CMS/PKCS#7 que pide WSAA se hace con `node-forge` (`AfipWsaaClient`), no shelleando a un binario `openssl` — para que funcione igual en cualquier host sin depender de qué tenga instalado. A diferencia del stub de email (que no-opea en silencio), `StubAfipPadronService` lanza un error claro: la búsqueda la dispara un click del usuario, no es un efecto de fondo, así que el frontend necesita algo real que mostrar. La condición IVA que devuelve AFIP es best-effort (deriva de `datosRegimenGeneral.impuesto`/`datosMonotributo`, categorías que la propia AFIP cambia con el tiempo) y **no se persiste** — sólo se muestra en el momento de la búsqueda, no hay campo nuevo en `Company` para esto todavía (ver pendientes).

## Setup local de ESTA máquina (Windows, sin Docker)

Docker Desktop no corre en este Windows (build 18362, muy vieja para WSL2). Se instaló PostgreSQL nativo en su lugar:

- Quedaron **dos instancias** instaladas sin querer: PostgreSQL 18 en el puerto 5432 (instalada a mano, usuario `postgres`/`postgres`, sin usar), y PostgreSQL 16 en el puerto **5433** (instalada por mí vía winget — **esta es la que usamos**).
- `.env` (gitignored, no viaja con git) ya apunta al puerto 5433.
- Rol `plexo_app` creado a mano corriendo `docker/postgres-init/01-init-roles.sql`.
- Base `plexo` creada, las 10 migraciones aplicadas (la última, `companies_and_people`, agregada 2026-07-21), seed corrido.
- Login de prueba: `tenantId=79dcca57-3830-4f17-af13-c000c0c0d0df`, `owner@demo.plexo` / `changeme123`. También existe `colega@demo.plexo` / `changeme123` (mismo tenant, rol SALES) — se creó a mano (no vía seed) sólo para probar presencia online entre dos usuarios reales; sólo en esta base local.

**Si retomás en otra computadora**: si tiene Docker andando, usar el `docker-compose.yml` normal del repo. Si no, repetir el proceso de Postgres nativo (instalar, crear base `plexo`, correr `docker/postgres-init/01-init-roles.sql`, `npx prisma migrate deploy`, seed) y ajustar tu propio `.env` — no se sincroniza entre máquinas.

## Módulos / features completos (actualización 2026-07-20/21)

| Commit | Qué es |
|---|---|
| `b4ab08e` | **Tablero en tiempo real** — login, dashboard con KPIs, stock por depósito, últimas facturas, gráfico de ventas 7 días, alertas de stock bajo mínimo. WebSockets socket.io (port 3001) con salas por tenant. TanStack Query + recharts en frontend. |
| `de65fc3` | Fix: CORS habilitado para `localhost:4200` en la API Fastify |
| `67a0ed9` | Fix: login guardaba `res.data.access_token` (no existe) en vez de `res.data.accessToken` — el token quedaba como string `"undefined"` y todo el dashboard fallaba con 401. Encontrado al probar el tablero por primera vez contra una base real. |
| `f3334ec` | **Frontend — pantalla de Inventario** (`apps/web/src/app/inventory`): tabla de artículos/variantes con buscador en tiempo real (client-side) y filtro por categoría, stock consolidado por depósito (columnas dinámicas según los depósitos que existan), modal para registrar movimientos de stock (atómico, vía el endpoint ya existente `POST /inventory/movements`). Se extrajo `AppShell` (`apps/web/src/components/AppShell.tsx`) como layout compartido con nav — el Tablero ahora lo usa también. Backend: se agregó `GET /inventory/categories` (no existía) y se enriqueció `GET /inventory/articles` para traer categoría + stock por depósito por variante (antes no traía nada de stock). |
| `36ce6d1` | **Auto-posteo contable** (lo más crítico del roadmap): `AccountingService.postInvoiceJournalEntry()` (`libs/modules/accounting`) resuelve (o crea, si no existen) 3 cuentas del sistema por código fijo — `1.1.02` Deudores por Ventas (ASSET), `4.1.01` Ventas (INCOME), `2.1.03` IVA Débito Fiscal (LIABILITY) — y postea el asiento estándar: débito AR por el total, crédito Ventas por el subtotal, crédito IVA por el impuesto (omitida si no hay impuesto). Siempre balanceado por construcción (`total = subtotal + taxTotal`); se salta el posteo entero si el total es cero. `SalesService` (`apps/api/src/app/sales`) ahora inyecta también `AccountingService` y llama a esto justo después de crear la factura — mismo patrón que ya usaba para componer Facturación+Inventario, mismo `getTenantDb()` compartido, así que es atómico gratis (si el asiento no balancea o inventario falla, se revierte todo junto). Probado end-to-end: `getIncomeStatement` (que antes daba vacío siempre) ahora refleja ventas reales, con y sin IVA. |
| `d01bdf2` | Doc: decisión de dejar la venta sin factura (`SALE_OUT` directo sin `invoiceId`) con asiento manual por ahora — ver decisión de arquitectura arriba. |
| `ff3ea58` | **Notas de crédito ahora revierten el asiento** — cierra el hueco que dejaba la iteración anterior. `AccountingService.reverseInvoiceJournalEntry(invoiceId)` busca el `JournalEntry` por `invoiceId` (es `@unique` en el modelo) y lo revierte reusando `createReversingEntry` (swap DEBIT/CREDIT). No-op si la factura nunca tuvo asiento (total cero, o previa al auto-posteo). **Cambio de API**: `POST /api/invoicing/credit-notes` se eliminó (nada más lo llamaba — verificado, ni frontend ni tests) y se reemplazó por `POST /api/sales/credit-notes`, que compone `InvoicingService.createCreditNote` + la reversión contable en `SalesService.voidSale()` — mismo patrón/atomicidad que ya usaba `createSale()`. Se consolidó a un solo camino a propósito: si hubiera quedado el endpoint viejo en paralelo, alguien podría acreditar una factura sin que se revierta el asiento, reabriendo el mismo hueco que se acaba de cerrar. No toca stock (una nota de crédito no repone inventario todavía — fuera de alcance de este cierre puntual, ver pendientes). |
| `7d53b79` | **Email real (Resend)** — `ResendEmailSender` (`libs/modules/invoicing`) implementa el `EmailSender` port que ya existía (antes solo el stub de consola). Decisión (con el usuario, 2026-07-21): **configuración global por `.env`** (`RESEND_API_KEY` + `EMAIL_FROM`), igual que `JWT_SECRET` — no por-tenant, así que no hace falta sector de administración ni cifrado de secretos en la base todavía. Si no están seteadas, `InvoicingModule` cae solo al stub de consola con un warning (`RESEND_API_KEY/EMAIL_FROM not set...`) — dev/local sigue funcionando sin cuenta de Resend. Fallos de envío se loguean, no rompen la request. De paso arreglé el lint preexistente de `invoicing` (`@nestjs/event-emitter` faltaba en su `package.json`). **Confirmado con cuenta real** el mismo día: el usuario cargó su `RESEND_API_KEY` propio en el `.env` local (no versionado) y probamos el envío de verdad contra `albionsistemas@gmail.com` — llegaron los correos. |
| `e9a02f2` | **Cron jobs de Cuentas a Cobrar** — `ReceivablesSchedulerService` (`apps/api/src/app/scheduler`) corre `@Cron(CronExpression.EVERY_DAY_AT_1AM)`, recorre todos los tenants (vía `list_tenant_ids()`, ver decisión de arquitectura arriba) y, por cada uno, dentro de su propia transacción (`withTenantContext`, el mismo helper que ya usaba el login): primero llama al nuevo `ReceivablesService.listInvoicesBecomingOverdue()` (la misma condición que `refreshOverdueStatuses` está por voltear — status `ISSUED`/`PARTIALLY_PAID` con `dueDate` vencido — capturada ANTES del update para saber exactamente qué facturas "recién" vencen), corre `refreshOverdueStatuses()` para marcarlas `OVERDUE`, y manda una alerta por email (`InvoicingService.sendOverdueInvoiceAlert()`, nuevo método público que envuelve el `EmailSender` sin exponer el token fuera del módulo) a cada cliente con email cargado. Se alerta **una sola vez** por factura — al día siguiente ya no vuelve a aparecer en `listInvoicesBecomingOverdue` porque su status ya es `OVERDUE`, no un nag diario. Un tenant que falla se loguea y no aborta el resto del barrido. **Probado end-to-end de verdad**: cambié el cron a cada 10 segundos temporalmente, creé una factura con vencimiento pasado, vi el log marcarla `OVERDUE` en el siguiente tick y confirmé que no se re-alertó en los ticks siguientes — después revertí a diario antes de commitear. |
| `67bc20f` | **Perfil de usuario + presencia online + log de actividad**, tres piezas relacionadas: (1) `User` ganó `name`, `avatarUrl` (solo URL, ver decisión arriba), `showOnlinePresence` (opt-in, default `true`); nuevos `GET/PATCH /auth/me` y `POST /auth/change-password`; pantalla `/profile` (avatar con fallback de iniciales, datos de cuenta, toggle de presencia, cambio de contraseña) usando el `AppShell` compartido. (2) **Presencia online**: `DashboardGateway` (el WebSocket que ya existía) ahora trackea en memoria qué usuarios de cada tenant están conectados (`Map<tenantId, Map<userId, {socketIds}>>` — varias pestañas del mismo usuario cuentan como una sola presencia, offline recién cuando se cierra la última), emite `presence.online`/`presence.offline` a los demás del tenant y `presence.snapshot` al que se conecta. Respeta el toggle: si `showOnlinePresence=false`, el usuario se une igual a la sala (recibe stock/facturas) pero nunca aparece como online para nadie. `AppShell` muestra "N compañeros en línea" en el header. (3) **Log de actividad** (`user_activity_log`, ver decisión arriba): interceptor global para escrituras + logging directo de intentos de login (éxito/fallo) en `AuthService`. |
| `c300c19` | **`Company`/`Person` (reemplaza `Customer`) + pantallas de Facturación y Cuentas a Cobrar** — ver las 3 decisiones de arquitectura de arriba (Company multi-rol, Person sólo cliente/proveedor, sucursal formaliza el punto de venta). Resumen de lo que se tocó: <br>**Backend**: nuevo módulo `@plexo/companies` (`libs/modules/companies`) — CRUD de `Company` (con `roles: CompanyRoleType[]`, reemplazo completo al hacer `PATCH`) y de `Person`; `InvoicingService`/`ReceivablesService`/`ReportsSalesService` repuntados de `Customer` a `Company` (mínimo diff gracias al truco de mantener el nombre de campo `customer` en las relaciones); `InvoicingController` perdió `POST/GET /customers` (se mudó a `/companies`); `CreateSaleDto` cambió `pointOfSale: string` por `branchId: string`, resuelto por `SalesService` contra `Company.pointOfSaleNumber`. <br>**Frontend**: pantalla `/invoicing` (tabla de facturas con búsqueda, modal de alta con selector de cliente/sucursal/depósito/moneda y líneas dinámicas, modal de cobro, modal de nota de crédito) y `/receivables` (antigüedad de saldos, saldos+límite de crédito, modal de estado de cuenta por cliente, botón "Actualizar vencidos"). Ambas usan `AppShell` + TanStack Query, mismo estilo que Inventario. <br>**Probado end-to-end de punta a punta en Chrome**: creé una sucursal con PV propio y una empresa con rol CLIENTE+PROVEEDOR simultáneo vía API, agregué un contacto (`Person`) y confirmé que se rechaza para una empresa que sólo es sucursal, emití una factura nueva desde la pantalla (resolviendo el PV de la sucursal, no tipeado a mano), cobré esa factura desde la UI (pasó a "Pagada"), emití una nota de crédito desde la UI y confirmé en `/accounting/trial-balance` que el asiento se revirtió, y verifiqué que Cuentas a Cobrar (antigüedad, saldos, estado de cuenta) siguió funcionando con los datos migrados. |
| `c300c19` (misma tanda) | **Pantalla de Empresas** (`/companies`): tabla con búsqueda por nombre/CUIT y filtro por rol (Todas/Clientes/Proveedores/Sucursales), modal de alta/edición (mismo componente para ambos casos) con checkboxes de rol, límite de crédito (sólo visible si el rol incluye CLIENTE) y punto de venta (sólo si incluye SUCURSAL), y modal de detalle con la lista de contactos (`Person`) de esa empresa + formulario para agregar uno nuevo — oculto si la empresa no es cliente ni proveedor, coherente con la regla que ya tenía el backend. Ahora sí se puede dar de alta cliente/proveedor/sucursal completamente desde la UI, sin pasar por la API a mano (el modal de nueva factura sigue sin poder crear cliente/sucursal inline — ver pendientes). Probado en Chrome: filtro por rol, alta de una empresa proveedor, agregar contacto, edición de límite de crédito. |
| `ba5d72b` | **Pantalla de Contabilidad** (`/accounting`): antes de construir esto se investigó qué usa un contador contemporáneo en una PyME argentina (RG 3685/Libro IVA Digital, ARCA como sucesor de AFIP, qué tan seguido se mira cada informe) — decisión con el usuario: alcance acotado a las 4 vistas de uso diario/mensual que ya tienen datos reales gracias al auto-posteo (`36ce6d1`), dejando explícitamente afuera Libro IVA Digital y conciliación bancaria (necesitan integración fiscal/bancaria real, no tiene sentido construir la pantalla antes de esa integración). Las 4 tabs, todas sobre endpoints que ya existían (no se tocó el backend): **Plan de Cuentas** (tabla + alta de cuenta), **Libro Diario** (asientos expandibles mostrando sus líneas, indicador de reversión, alta de asiento manual con validación de balance debe=haber en vivo — el botón de postear queda deshabilitado hasta que balancea), **Libro Mayor** (selector de cuenta + saldo corriente acumulado línea a línea), **Balance de Sumas y Saldos** (con fila de totales). Mismo patrón que el resto (`AppShell` + TanStack Query, sin Tremor). Probado en Chrome de punta a punta: los 9 asientos auto-posteados por ventas/notas de crédito previas se ven correctamente (incluida la reversión con débito/crédito invertidos), el balance de sumas y saldos cierra ($15680 = $15680), el mayor de "Deudores por Ventas" refleja el saldo bajando después de la nota de crédito, se creó una cuenta nueva ("1.1.03 Caja") desde la UI, y se posteó un asiento manual balanceado ("Cobro en efectivo de cliente", débito Caja/crédito Deudores por Ventas $500) que apareció correctamente en el libro diario. |
| `f096a6e` | **Pantallas de Impuestos y Reportes** (`/taxes`, `/reports`) — cierra el roadmap original de 9 módulos, todas sobre endpoints backend que ya existían (`@plexo/taxes`, `@plexo/reports-pnl`, `@plexo/reports-sales`, `@plexo/reports-financial`), sin tocar su lógica de negocio salvo el fix de rango de fechas descrito abajo. <br>**Impuestos**: una sola tabla con todo el historial de `TaxDefinition` (vigentes y versiones cerradas, ordenadas por código y luego por vigencia descendente), alta de impuesto nuevo (porcentual/monto fijo/fórmula, con el flag "delegado al contador"), y "Revisar" por fila vigente — abre un modal que llama a `POST /taxes/definitions/revise`, cerrando la versión actual y creando una nueva sin tocar las facturas ya emitidas. Las filas históricas no muestran el botón de revisar. <br>**Reportes**: pantalla con 3 tabs — **Resultados** (tarjetas de resumen de facturación + estado de resultados por cuenta INCOME/EXPENSE del libro mayor, con filtro de fecha), **Ventas** (ventas por cliente y por producto, mismo filtro de fecha), **Financiero** (alta de cuentas financieras — banco/MercadoPago/PayPal/efectivo —, registro de movimientos firmados, y conciliación manual: resumen de saldo contable/conciliado/sin conciliar por cuenta, con botón "Conciliar" por movimiento pendiente). Todas con filtro "Desde/Hasta" reusable (`DateRangeFilter`, default al mes en curso). <br>**Bug real encontrado y arreglado en el camino**: al filtrar "Hasta" con la fecha de hoy, ningún reporte de fecha (`reports-pnl`, `reports-sales`) mostraba las facturas emitidas *ese mismo día* — `new Date("2026-07-21")` parsea a medianoche UTC de ese día, así que cualquier factura emitida más tarde ese día quedaba afuera del filtro `lte`. Se arregló en `defaultRange()` de ambos servicios normalizando `to` a las 23:59:59.999 **UTC** (`setUTCHours`, no `setHours` — la primera versión del fix usaba `setHours`, que interpreta en la zona horaria local del proceso y desplazaba el corte varias horas en la dirección equivocada; también se corrigió el cálculo del default de `from` — primer día del mes — para usar getters/`Date.UTC` en vez de mezclar UTC y hora local). Detectado y confirmado en Chrome: el tablero mostraba "Facturado hoy $12060 / 7 facturas" pero Reportes → Resultados con rango 01/07–21/07 mostraba 0 facturas hasta aplicar el fix. <br>**Probado en Chrome de punta a punta**: Impuestos — creé "Ingresos Brutos CABA" (3%, delegado al contador) y lo revisé a 3.5%, confirmando que la versión vieja queda con vigencia cerrada y sin botón de revisar. Reportes — con el fix aplicado, Resultados mostró las 7 facturas reales ($12060 total, Ventas $7600 neto de la nota de crédito); Ventas mostró el desglose real por cliente y por producto; Financiero — creé una cuenta bancaria, registré un movimiento de $5000, lo concilié y confirmé que "Sin conciliar" bajó a $0 y "Conciliado" subió a $5000. |
| `de869b1`, `6cedd85`, `e067c03` | **Menú de usuario con avatar + modo claro/oscuro en toda la app** (2026-07-22) — el header ya no tiene un link de texto "Cerrar sesión": ahora es un avatar circular (foto si hay `avatarUrl`, si no iniciales) arriba a la derecha que despliega un menú con Perfil, un ícono de tema (sol/luna, alterna claro/oscuro, sin texto — 2 iteraciones de feedback del usuario para llegar a "solo el ícono") y Cerrar sesión. `ThemeProvider` (`apps/web/src/providers/ThemeProvider.tsx`) guarda la preferencia en `localStorage`, default a `prefers-color-scheme` la primera vez, aplica una clase `dark` en `<html>` (Tailwind v4 con `@custom-variant dark (&:where(.dark, .dark *));` en `global.css` para que sea manual y no sólo del sistema), y un script inline en `layout.tsx` la aplica antes de hidratar (con `suppressHydrationWarning` en `<html>`, si no React tira un warning de mismatch server/cliente). <br>**Las 32 pantallas/componentes** que tenían la paleta hardcodeada en oscuro (slate-900/950 fijo) se migraron a pares `light dark:` con un script de una sola vez (`node`, mapeo espejado de la escala: `slate-900 dark:slate-900` → light usa `slate-100`, etc. — 50↔950, 100↔900, 200↔800... así el oscuro queda pixel a pixel igual a como estaba y el claro es la inversión natural). De paso quedó themeado el tooltip del gráfico de Ventas del Tablero (colores inline de Recharts, fuera del alcance del script) y se arregló un error de tipos preexistente en su `formatter` que rompía `next build` (no bloqueaba `next dev`, por eso no se había notado). |
| `bc9f2c6` | **Autocompletar Empresa por CUIT vía padrón de AFIP** (2026-07-22) — botón "Buscar en AFIP" en el alta/edición de Empresa (`/companies`) que completa el nombre y muestra condición IVA + domicilio fiscal a partir del CUIT. Ver decisión de arquitectura abajo (AFIP padrón vs. AFIP WSFE) — son dos integraciones AFIP distintas, no confundir con el punto 1 de pendientes (CAE real). |
| `8e58f7b` | **Checklist de "primeros pasos" + panel de detalle de factura** (2026-07-22, dos recomendaciones del análisis contra un competidor, ver punto 17 de pendientes) — <br>**Checklist** (`OnboardingChecklist.tsx`, en el Tablero): 4 pasos (empresa, artículo, factura, perfil) derivados de las mismas listas que ya usa cada pantalla, sin endpoints nuevos; se auto-oculta cuando los 4 están completos o el usuario lo descarta (`localStorage`, no hay preferencia de usuario en la base para esto). Probado en Chrome: en el tenant demo (que ya tiene todo cargado) detecta los 4 pasos completos y se descarta solo. <br>**Panel de detalle de factura** (`InvoiceDetailPanel.tsx`, en `/invoicing`) — primer panel lateral deslizante de la app (hasta ahora todo "detalle" era modal centrado, ver decisión de arquitectura abajo): línea a línea con artículo/SKU/descuento, totales, historial de cobros y notas de crédito. Backend: se amplió el `include` de `InvoicingService.getInvoice()` (antes sólo `lines`) para sumar `receipts`, `creditNotes` y el artículo/variante de cada línea — mismo endpoint `GET /invoicing/invoices/:id` que ya existía, sin ruta nueva. Probado en Chrome contra datos reales: factura pagada con cobro y nota de crédito, y factura sin cobros ("Sin cobros registrados"). Sólo se agregó a Facturación por ahora, no a Cuentas a Cobrar (ver punto 10 de pendientes). |
| `26f37e1` | **Tabs Resumen/Gestión en Facturación y Cuentas a Cobrar + densidad compacta opcional** (2026-07-22) — cierra las 5 recomendaciones del análisis contra un competidor (punto 17 de pendientes). <br>**Tabs** (mismo patrón que ya usan Contabilidad y Reportes — tab bar simple, `useState` local, sin ruteo): Facturación quedó **Resumen** (KPIs de facturación + facturas por estado, derivados client-side de la misma lista que ya carga Gestión, sin endpoints nuevos) / **Gestión** (la tabla operativa de siempre, sin cambios de comportamiento). Cuentas a Cobrar no tenía una división natural entre "resumen" y "gestión" (ambas tablas eran de sólo lectura con el mismo click-para-extracto) — se decidió **Resumen** = antigüedad de saldos (analítico) / **Gestión** = saldos + límite de crédito + el botón "Actualizar vencidos" (la única acción real de la pantalla). <br>**Densidad compacta** (`DensityProvider.tsx`, mismo patrón `localStorage` que `ThemeProvider`; toggle icon-only en el menú de usuario, junto al de tema — mismo aprendizaje de "sólo el ícono, sin texto" de esa feature) para Libro Diario, Libro Mayor y la tabla de Facturación: reduce el padding vertical de filas/headers para ver más filas sin scrollear, sin tocar el resto de la app. Probado en Chrome: ambas pantallas con sus dos tabs, densidad activada/desactivada en las 3 tablas objetivo, y el modal de estado de cuenta de Cuentas a Cobrar sigue abriendo bien desde la tabla de Gestión. |
| `9597bab` | **Empresas: desactivar en vez de borrar (`Company.active`)** (2026-07-22) — ver decisión de arquitectura abajo. `Company` ganó `active: Boolean @default(true)`; `listCompanies` la excluye por default (`?includeInactive=true` para verlas/reactivarlas); el toggle activar/desactivar reusa el `PATCH /companies/:id` existente (`UpdateCompanyDto.active`), sin ruta nueva. `Person` (contactos) sigue con `DELETE` real — no tiene significancia fiscal ni nada que referencie. `InvoicingService.createInvoice` y `SalesService.createSale` rechazan (400) operar sobre una empresa/sucursal inactiva; `receivables` y `reports-sales` quedaron deliberadamente sin este filtro (antigüedad/estadísticas históricas no deben desaparecer si la empresa se desactiva después). Frontend: checkbox "Mostrar inactivas" en `/companies`, badge "Inactiva" + fila atenuada en la tabla, botón único Activar/Desactivar en el modal de detalle (reemplazó el flujo viejo de confirmar-borrado). Probado con `curl` (alta, listado con/sin `includeInactive`, activar/desactivar, rechazo de factura contra empresa inactiva) y en Chrome (toggle, badge, checkbox) — el toggle vía navegador automatizado pegó el mismo 503 cosmético ya documentado abajo para `PATCH`, confirmado sin problema real contra la API directo. |
| `2e1da37` | **Notas de crédito ahora reponen stock** (2026-07-22) — cierra el punto 4 de pendientes. `SalesService.voidSale()` (además de crear la nota de crédito y revertir el asiento) lee los `StockMovement` tipo `SALE_OUT` originales de esa factura (`stockMovement.findMany({ invoiceId, type: 'SALE_OUT' })` — ahí vive el `warehouseId` real de cada línea, el `Invoice`/`InvoiceLine` no lo guarda) y registra un `RETURN` por cada uno (tipo que ya existía en `MovementType`, sólo no se usaba desde ningún lado todavía), mismo `warehouseId`/cantidad, `sourceType: 'CREDIT_NOTE'`. Como `CreateCreditNoteDto` sigue siendo "reversión total" (v1, ver su docstring), esto siempre repone la factura completa, nunca una devolución parcial. Probado con `curl` contra la base real: venta de 3 unidades de "Agua mineral 500ml" (Depósito Central 23→20), nota de crédito sobre esa factura (20→23) — stock vuelve exacto. |
| `1a3b315` | **Crear cliente/sucursal inline desde el modal de nueva factura** (2026-07-22) — cierra el punto 2 de pendientes. `CompanyFormModal` (`/companies`) ganó dos props opcionales: `defaultRoles` (para abrirlo con "Sucursal" en vez de "Cliente" pre-tildado, cuando se crea sin editar una empresa existente) y `onSaved` (callback con la empresa creada/actualizada, además del invalidate+close que ya hacía) — así un caller que lo abre "flotando" dentro de otro formulario puede auto-seleccionar el resultado sin duplicar el formulario. `NewInvoiceModal` (`/invoicing`) suma "+ nuevo cliente" y "+ nueva sucursal" junto a esos selects; al guardar, el modal de empresa se cierra y el nuevo id queda seleccionado en el select correspondiente, sin perder el resto de la factura en progreso (ambos modales se apilan, mismo patrón ya usado para `InvoiceDetailPanel` sobre `StatementModal`). El bloqueo total de "hace falta cliente/sucursal/depósito" se acotó a sólo depósito (sin atajo inline todavía) ya que cliente/sucursal ahora se resuelven sin salir del formulario. Probado en Chrome de punta a punta: creación inline de "Cliente Inline Test SRL" y "Sucursal Inline Test (0003)" desde dentro del modal de nueva factura, ambos auto-seleccionados, factura emitida correctamente contra ellos. |
| `1a0e816` | **Listado completo de presencia online** (2026-07-22) — cierra el punto 9 de pendientes. `OnlineColleagues` (`AppShell.tsx`) pasa de ser un contador con tooltip nativo a un botón que abre un dropdown (mismo patrón click-fuera/Escape que ya usaba `UserMenu`) listando cada colega conectado con avatar de iniciales, nombre y email — los eventos `presence.snapshot`/`presence.online` ya traían esos datos, sólo faltaba mostrarlos. Se cierra solo si la lista se vacía mientras está abierto (el último colega se desconectó a mitad de sesión). **Verificado en Chrome con dos usuarios reales conectados a la vez (2026-07-23)**: `owner@demo.plexo` en `localhost:4200` y `colega@demo.plexo` en `127.0.0.1:4200` (mismo navegador, dos orígenes para tener `localStorage` — y por lo tanto sesión/token — separados; requirió sumar temporalmente `127.0.0.1:4200` a los `origin` de CORS de `main.ts` y `DashboardGateway`, revertido al terminar la prueba). Confirmado de punta a punta: el header de cada uno mostró "1 compañero en línea" en tiempo real apenas se conectó el otro, el dropdown mostró al otro usuario con nombre/email/avatar correctos en ambos sentidos, y al cerrar la pestaña de `colega` el contador de `owner` volvió a 0 y el dropdown (abierto en ese momento) se cerró solo. |
| `stock_movements_immutability` (migración) | **Kardex inmutable + saldo nunca negativo a nivel DB** (2026-07-23) — a pedido del usuario, tras auditar el módulo de Inventario contra un checklist externo de concurrencia/multi-tenancy para 100+ usuarios simultáneos. `stock_movements` (el kardex) era append-only sólo por convención de código (`InventoryService.recordMovement` nunca hace `update`/`delete`, pero nada en la base lo impedía) — a diferencia de `audit_log` y `journal_entries`, que ya tenían un trigger `BEFORE UPDATE OR DELETE ... RAISE EXCEPTION` para exactamente este caso. Se agregó el mismo trigger (`stock_movements_lock`) sobre `stock_movements`. De paso, se sumó `CHECK ("quantity" >= 0)` en `stock_ledger` — el saldo nunca negativo ya lo garantizaba `recordMovement()` con un `UPDATE ... WHERE quantity >= $delta` atómico (una sola sentencia, sin `SELECT ... FOR UPDATE` explícito: la condición de suficiencia vive en el propio `WHERE`, así que dos ventas compitiendo por la última unidad resuelven con `count=0` en la perdedora en vez de esperar un lock), pero eso era una garantía sólo de la aplicación, no de la base. Migración puramente aditiva: no toca `schema.prisma` (Prisma no necesita saber de triggers/CHECKs para construir queries), no requirió cambios de código, probada contra la base real — `UPDATE`/`DELETE` directo sobre `stock_movements` ahora rechaza con el mensaje del trigger, un `UPDATE` a cantidad negativa en `stock_ledger` rechaza con el `CHECK`, y un movimiento real vía `POST /inventory/movements` (el único camino que usa la app) se sigue insertando sin problema. `nx test` de `inventory`+`database` 100% verde. **Quedó pendiente de decidir, no implementado todavía**: los unique constraints de `stock_ledger`/`minimum_stock` (`@@unique([warehouseId, articleVariantId])`) no arrancan con `tenantId` — no es un problema hoy (aislamiento es vía RLS, no particionamiento), pero si en el futuro se quisiera particionar alguna de estas tablas por `tenantId`, Postgres exige que la partition key esté incluida en cualquier unique/PK — hoy no lo está. Cambiarlo es más invasivo (la compound key `warehouseId_articleVariantId` se referencia en varios lugares de `inventory.service.ts`), así que se dejó para cuando/si el particionamiento sea una necesidad real, no preventivamente. |
| `company_afip_tax_condition` (migración) | **Persistir condición IVA / domicilio fiscal de AFIP en `Company`** (2026-07-23) — cierra el punto 16 de pendientes. `Company` ganó `taxCondition`/`fiscalAddress` (`String?`, migración puramente aditiva); `CreateCompanyDto`/`UpdateCompanyDto` los aceptan como texto libre opcional. `CompanyFormModal` ahora guarda lo que devuelve "Buscar en AFIP" (antes sólo aparecía en un mensaje efímero que se perdía al cerrar el modal) y lo precarga al editar una empresa que ya lo tenía guardado; `CompanyDetailModal` los muestra en la vista de detalle, sólo si están presentes. No re-validado ni normalizado (AFIP cambia sus categorías con el tiempo, mismo criterio ya documentado para `AfipPadronData.taxCondition`) y todavía no dispara ninguna lógica (elegir automáticamente factura A/B/C sigue sin usar este campo). Probado de punta a punta: creé una empresa vía API con ambos campos, confirmé la persistencia contra la base real, y en Chrome verifiqué que el modal de detalle y el de edición (con precarga) los muestran correctamente. `nx lint,test,build` de `companies`/`database`/`web` 100% verde. |
| `tenant_settings_and_recurring_reminders` (migración) + `@plexo/tenant-settings` | **Recordatorio recurrente de Cuentas a Cobrar, configurable por tenant** (2026-07-23) — cierra el punto 6 de pendientes. Hasta ahora la alerta de vencimiento era una sola vez por factura (al cruzar a `OVERDUE`); ahora es opt-in por tenant. <br>**Nuevo módulo `@plexo/tenant-settings`** (primer lib nueva desde `companies`, escafoldeada a mano copiando la config de `receivables` — no hay skill de generadores Nx instalado en este entorno pese a lo que dice `CLAUDE.md`): `TenantSettings` (1 fila por tenant, creada lazy en el primer `PATCH`) con un solo campo por ahora, `arReminderIntervalDays Int?` (`null` = comportamiento original de alerta única). `GET /tenant-settings` abierto a cualquier rol autenticado, `PATCH` restringido a OWNER/ADMIN (es política de negocio del tenant, no una preferencia personal como tema/densidad). <br>**Kardex de recordatorio**: `Invoice` ganó `lastOverdueReminderAt DateTime?` — requirió tocar el trigger `invoice_fiscal_lock` (whitelist de columnas que pueden cambiar en una factura ya emitida con CAE) para sumar esta columna a la lista permitida junto a `balanceDue`/`status`, si no cualquier intento de recordatorio recurrente sobre una factura ya facturada fiscalmente hubiera reventado contra el propio trigger de bloqueo fiscal. <br>**`ReceivablesSchedulerService`** se refactorizó: el cuerpo del barrido diario (`runReminderSweepForCurrentTenant`) quedó separado de su wrapper de `withTenantContext` por tenant, así el mismo método lo puede llamar tanto el cron (`@Cron` diario, un tenant a la vez) como un endpoint HTTP nuevo (`POST /receivables/reminders/run-now`, ya corriendo dentro del contexto de tenant de la request). Las facturas recién vencidas y las que califican para recordatorio recurrente (`ReceivablesService.listInvoicesNeedingRecurringReminder`, gateado por `arReminderIntervalDays`) se marcan como "recién recordadas" **juntas, en una sola llamada al final** (`markReminderSent`) — si se marcaran por separado, una factura recién vencida hoy calificaría de nuevo para el recordatorio recurrente mañana mismo, sin importar el intervalo configurado. `POST /receivables/reminders/reset` reinicia el reloj de todas las facturas OVERDUE sin mandar ningún mail (para postergar el próximo aviso sin re-notificar a nadie), `GET /receivables/reminders/status` devuelve el estado (activado/desactivado, intervalo, próxima corrida estimada del cron — un valor fijo calculado en código, ya que `@nestjs/schedule` no expone "próxima corrida" y no valía la pena traer su `SchedulerRegistry` sólo para leer eso). <br>**Frontend**: en un primer diseño esto vivía en una pantalla nueva `/preferences` (accesible desde el menú de usuario, sólo para OWNER/ADMIN) — a pedido del usuario después de verlo, se movió **todo** a una tercera pestaña "Recordatorios" dentro de `/receivables` (junto a Resumen/Gestión, mismo patrón de tabs), porque como preferencia de Cuentas a Cobrar específicamente no tenía sentido vivir en una pantalla separada. `/preferences` quedó como placeholder vacío para si en algún momento aparecen preferencias realmente generales (no ligadas a un módulo). La pestaña Recordatorios tiene: checkbox activar/desactivar + selector de intervalo (3/5/10 días o "Otra" con input numérico), tarjeta de estado (intervalo activo, cuenta regresiva a la próxima corrida del cron) y dos botones, "Ejecutar recordatorio ahora" y "Reiniciar conteo (sin enviar mails)". <br>**Probado de punta a punta contra la base real** (`curl`, ya que el `PATCH` vía navegador automatizado reprodujo una vez más el 503/silent-fail cosmético ya documentado varias veces en este archivo — confirmado sin problema real contra la API directo, y los `POST` sí funcionaron bien vía Chrome): activar con intervalo custom, `run-now` idempotente (a los pocos segundos de correr, una segunda corrida da `recurring: 0` porque el reloj ya se acababa de reiniciar), `reset` sin disparar ningún email, y el trigger de bloqueo fiscal aceptando `lastOverdueReminderAt` en una factura con CAE real mientras sigue rechazando cualquier otra columna. `nx lint,test,build` de `tenant-settings`/`receivables`/`api`/`web` (y `nx run-many --all`) 100% verde — se sumó `scope:tenant-settings` a `eslint.config.mjs` (`@nx/enforce-module-boundaries`) para la lib nueva. |

**Nota sobre Tremor**: el brief pedía Tremor para estos componentes, pero `@tremor/react` tiene como peer dependency `react@^18` y el proyecto está en React 19 — instalarlo rompía peer deps. Se construyó con Tailwind puro, seteando el mismo estilo (dark, slate/indigo) que ya usa el Tablero.

**Datos de demo**: el seed (`libs/shared/database/prisma/seed.ts`) solo crea tenant + usuario, nada de inventario/moneda/clientes/empresas. Para poder probar las pantallas se cargó a mano vía API (no vía script versionado), sólo en la base local de esta máquina: depósitos, categorías, artículos con y sin impuesto, una moneda base, dos clientes migrados desde el viejo seed de Customer, y ahora además una sucursal ("Casa Central", PV `0002`) y una empresa con doble rol CLIENTE+PROVEEDOR ("Distribuidora Multi SA", con un contacto "Laura Gómez"). Nada de esto viaja con git ni afecta otras máquinas.

**Alcance del auto-posteo — lo que sigue faltando**:
- Las cuentas del plan (`1.1.02`, `4.1.01`, `2.1.03`) son códigos por convención, no configurables desde ningún lado todavía — si un tenant ya tenía cuentas propias con esos códigos, se reusan; si no, se crean automáticamente la primera vez.
- No hay costo de mercadería (COGS) — `getIncomeStatement` sigue sin gastos del lado de ventas, sólo ingresos.
- ~~Una nota de crédito revierte el asiento pero no repone el stock~~ — resuelto (2026-07-22, ver tabla de arriba).
- Venta sin factura (`SALE_OUT` directo) sigue siendo asiento manual — decisión explícita, ver arriba.

**Bugs preexistentes — los 4 que quedaban, arreglados (`c65c6c7`, 2026-07-22)**. `nx run-many -t lint,test,build --all` da 100% verde por primera vez en todo el workspace:
- ~~`inventory.service.spec.ts`: 2 tests rotos~~ — `InventoryService` ganó un `EventEmitter2` inyectado y ahora relee el ledger vía `stockLedger.findUnique()` antes de emitir `stock.updated` (para el tablero en tiempo real, `b4ab08e`); los mocks nunca se habían actualizado. Se agregó `makeEventEmitter()` (mismo patrón que ya existía en invoicing) y el stub de `findUnique` en los dos tests del camino feliz.
- ~~`invoicing.service.spec.ts`: 1 test roto~~ — mismo patrón: `InvoicingService` también gana un `EventEmitter2` (evento `invoice.created`). Se agregaron los 16 `makeEventEmitter()` que faltaban en el archivo, y el mock de la factura creada sumó `issueDate`/`tenantId`/`customerName`/`status` — el `emit` lee `issueDate.toISOString()`, que no existía en el mock viejo.
- ~~`inventory:lint`: error de `@nx/dependency-checks`~~ — mismo que ya se había arreglado en `invoicing` hace unos días, faltaba declarar `@nestjs/event-emitter` en `libs/modules/inventory/package.json`.
- ~~`apps/web/specs/index.spec.tsx`~~ — el scaffold de Nx intentaba renderizar `Home()`, que sólo hace `redirect('/login')`: eso no es una UI renderizable, es un side effect. Se reemplazó por un test que mockea `next/navigation` y confirma que se llama a `redirect('/login')`.

**Bug real encontrado y arreglado (`e216273`), no preexistente — lo introdujo el panel de detalle de factura (`71d1704`) de esta misma sesión**: `InvoicingService.getInvoice()` con `include: { lines, receipts, creditNotes }` (3 relaciones to-many en un solo `findUnique`) quedaba colgado para siempre — sin error, sin timeout, el panel se quedaba en "Cargando..." indefinidamente. Causa: `getTenantDb()` es una única conexión de Postgres por request (`prisma.$transaction`, ver `withTenantContext` en `libs/shared/database`), y Prisma dispara relaciones to-many como queries de seguimiento concurrentes — `node-postgres` no soporta dos queries en vuelo sobre la misma conexión, así que la segunda queda esperando para siempre en vez de fallar (la única pista en el log del server: `Calling client.query() when the client is already executing a query is deprecated`). Con 1 sola relación to-many (`lines`, el include original) nunca se había notado — se agregaron 2 más de golpe para el panel y ahí se disparó. Fix: 3 queries secuenciales awaited una por una, nunca dos al mismo tiempo sobre el mismo cliente. **Detectado probando en Chrome recién ahora** (el testing de la sesión anterior había mostrado el panel funcionando — resulta que fue contra una respuesta ya cacheada/una carrera que no siempre se dispara; en la siguiente sesión, con la API reiniciada, se reprodujo consistente). Confirmado arreglado con `curl` directo (200 OK, ~0.4s) y en Chrome de punta a punta con una pestaña nueva.
- ~~**A vigilar**: `CompaniesService.getCompany()`~~ — revisado a pedido del usuario (`1f49763`). Con `include: { roles: true, people: true }` (2 relaciones to-many planas, sin anidamiento extra) **no reprodujo** el bug bajo prueba real: 8 requests secuenciales + 5 concurrentes contra una empresa con roles y contactos cargados, todas ~0.3s, sin el warning de `pg` en el log — la hipótesis es que hace falta además una relación anidada más profunda (como `lines→articleVariant→article` en el caso de arriba) para que Prisma dispare el batching concurrente; con 2 relaciones planas no pasa. Se aplicó igual el mismo fix preventivo (queries secuenciales) porque el usuario prefirió sacarse el patrón de encima aunque no se haya reproducido — mismo resultado confirmado por `curl` y en Chrome (modal de detalle de empresa, contactos completos). Si en algún momento aparece un "se queda cargando para siempre" en OTRA pantalla, el sospechoso es cualquier `include` con 2+ relaciones to-many, tengan o no anidamiento — no se puede descartar sólo por no tener nesting.

**Verificado end-to-end (perfil/presencia/actividad)**: probado en Chrome (perfil, avatar con iniciales, guardado de nombre) y con dos usuarios reales conectados por WebSocket a la vez (`owner@demo.plexo` + un `colega@demo.plexo` creado sólo para esta prueba, local, no seedeado) — confirmado que un tercer usuario ve el broadcast `presence.online`/`presence.offline` del otro, que varias conexiones del mismo usuario no disparan falsos "offline", y que la tabla `user_activity_log` efectivamente registra login (éxito y fallo, con IP) y un `PATCH /auth/me` real. Durante esta prueba encontré y arreglé un catch silencioso en `recordLoginAttempt` que se tragaba errores sin loguear nada — ahora loguea con `Logger.error` como el resto del código.

**Nota sobre la herramienta de automatización de Chrome usada para probar (no es un bug de PLEXO)**: en esta sesión, los requests `PATCH` disparados desde el navegador automatizado (perfil, editar empresa) fallaron de forma intermitente con un 503 que nunca aparece en el log del servidor — confirmado repetidas veces con `curl` contra el mismo endpoint, mismo payload, siempre 200. A veces el 503 era sólo cosmético (el dato quedaba guardado igual), y al menos una vez el request realmente no llegó a pegarle a la API. Nunca se vio con `GET`/`POST`. Es una falla de la capa de automatización/proxy de esta sesión de pruebas específicamente con `PATCH`, no del código — cada vez que se sospechó un bug real se confirmó (o se descartó) contra la API directo con `curl`. **Actualización 2026-07-22**: el mismo 503 cosmético se observó también con `DELETE` (borrado de contacto) — mismo patrón, mismo diagnóstico (confirmado con `curl`: 204, contacto efectivamente borrado). Sigue sin verse con `GET`/`POST`; el denominador común parece ser cualquier método mutante distinto de `POST`.

**Email real ya configurado en esta máquina**: `RESEND_API_KEY`/`EMAIL_FROM` están en el `.env` local (gitignored, no viaja con git) con una cuenta real del usuario, `from` sandbox `onboarding@resend.dev`. **En otra máquina hay que repetir el alta** (cuenta en resend.com + API key) — ver `.env.example` para el formato.

**Alcance de los cron jobs — lo que sigue faltando**:
- Sólo cubre Cuentas a Cobrar (vencimientos). AFIP/otros procesos periódicos, si aparecen, reusarían el mismo `list_tenant_ids()` + `withTenantContext` pero necesitan su propio `@Cron`.
- ~~La alerta es una sola vez al cruzar a OVERDUE, no un recordatorio recurrente~~ — resuelto (2026-07-23), opt-in por tenant desde Cuentas a Cobrar → Recordatorios (ver tabla de arriba). Sigue siendo un único `@Cron` diario compartido por todos los tenants — lo que cambia por tenant es sólo si esa corrida re-envía o no recordatorios de facturas ya `OVERDUE`.
- Verificado a mano (cron acelerado a 10s temporalmente, revertido antes de commitear) en esta base local; no corrió todavía en un ciclo diario real.

## Pendiente / próximos pasos

1. Integración AFIP WSFE real (hoy: CAE falso, `StubElectronicInvoicingService`) — **distinto** del padrón (búsqueda por CUIT, ver abajo), que sí tiene su integración real escrita.
2. Frontend: **ya están las 9 pantallas del roadmap original** (Inventario, Perfil, Facturación, Cuentas a Cobrar, Empresas, Contabilidad, Impuestos, Reportes + Tablero). ~~El modal de nueva factura sigue sin poder crear cliente/sucursal inline~~ — resuelto (2026-07-22, ver tabla de arriba). ~~Mojibake pre-existente en nombres con tildes ("Dep�sito Central", "Germ�n")~~ — corregido (2026-07-23). Diagnóstico confirmado: los dos valores tenían el byte de la tilde ya reemplazado por U+FFFD a nivel de fila (`ef bf bd` en hex) — corrupción irreversible ocurrida al cargar esos datos de prueba, no un bug de la API. Se probó además que **hoy el path real no corrompe nada**: un `PATCH /auth/me` vía `curl` en esta terminal de Windows sí reprodujo la corrupción (el propio `curl`/shell mangló el argumento no-ASCII), pero el mismo `PATCH` hecho con `fetch` nativo de Node (bytes UTF-8 controlados a mano) y por la UI real de `/profile` en Chrome guardó y devolvió el nombre con tildes intacto — así que la fuente del bug histórico fue la herramienta usada para cargar datos de prueba (`curl` en esta consola Windows), nunca el código de la app. `Germán Belvedere` (`users`) y `Depósito Central` (`warehouses`, sin endpoint de edición todavía, corregido por SQL directo) verificados en Chrome. Se escaneó el resto de tablas con nombres (`companies`, `articles`, `categories`, `people`, `article_variants`) — sin más ocurrencias.
3. ~~Arreglar los tests preexistentes rotos que quedan~~ — hecho (`c65c6c7`, 2026-07-22), ver nota arriba. Workspace 100% verde.
4. ~~Notas de crédito no reponen stock~~ — resuelto (2026-07-22, ver tabla de arriba).
5. Si algún día hace falta que cada tenant mande desde su propia cuenta/dominio: pasar de la config global por `.env` a un sector de administración con configuración por tenant — requiere modelo de configuración + cifrado de secretos en la base, no es trivial, no se hizo ahora a propósito (decisión 2026-07-21).
6. ~~Cron: alertas recurrentes (no solo una vez)~~ — resuelto (2026-07-23, ver tabla de arriba), configurable por tenant desde Cuentas a Cobrar → Recordatorios. Otros trabajos periódicos que necesiten `list_tenant_ids()` (ver nota arriba) siguen sin existir todavía — nada más lo necesita hoy.
7. **Súper-admin de plataforma** (charlado con el usuario 2026-07-21, explícitamente pospuesto): gestión de cuentas/planes de tenancy, variables globales vía UI, informes de uso de la plataforma (no de negocio por tenant), backups, importación desde Excel, cobro a los tenants (hoy Cuentas a Cobrar es para que CADA TENANT le cobre a SUS clientes, no para que la plataforma le cobre a los tenants). Nada de esto existe todavía — es, en la práctica, un segundo producto separado del ERP que cada tenant usa.
8. Log de actividad: sólo cubre requests mutantes autenticados + login. No cubre acciones de cron/sistema (no hay usuario/request HTTP detrás) ni lecturas (GET).
9. ~~Presencia online: no hay UI para ver el listado completo de quién está online~~ — implementado y verificado en Chrome con dos usuarios reales conectados a la vez (`1a0e816`, 2026-07-22; verificación 2026-07-23, ver tabla de arriba). Sin pendientes de este punto.
10. ~~Ni Facturación ni Cuentas a Cobrar muestran el detalle completo de una factura~~ — resuelto en ambas (`710e2bd`, 2026-07-22). `InvoiceDetailPanel` se movió a `apps/web/src/components` (ahora lo usan `/invoicing` y `StatementModal` de `/receivables`) con su prop `invoice` angostada a `{ id, documentLetter, number, customerName }` — lo único que necesita antes de que cargue el detalle completo, así `StatementModal` (que sólo trae un resumen de cada factura) lo puede abrir sin un fetch extra. Probado en Chrome: el panel se apila arriba del modal de estado de cuenta con datos reales.
11. ~~Empresas: no hay borrado (ni de empresa ni de contacto) todavía~~ — resuelto (`9597bab`, 2026-07-22): empresa se desactiva (`active=false`, ver decisión de arquitectura arriba), contacto se borra de verdad. Sin pendientes de este punto.
12. Contabilidad: quedó explícitamente afuera del alcance de la pantalla nueva el Libro IVA Digital (RG 3685/ARCA) y la conciliación bancaria — ambos necesitan integración fiscal/bancaria real primero (ver punto 1, AFIP/ARCA real). Las cuentas del sistema (`1.1.02`/`4.1.01`/`2.1.03`) siguen sin ser configurables desde la UI (ver nota de auto-posteo más arriba).
13. Reportes → Financiero: la "conciliación" es 100% manual (un botón que marca `reconciled=true`) — no hay import de extracto bancario ni matching automático contra movimientos. Es un libro de caja/banco con conciliación manual, no una integración bancaria real.
14. ~~Reportes → Resultados/Ventas: no hay atajos de UI para "mes anterior", "trimestre", etc.~~ — resuelto (2026-07-23). `DateRangeFilter` (compartido por ambas tabs) ganó 4 botones ("Este mes", "Mes anterior", "Este trimestre", "Este año") vía un nuevo prop `onPreset`; cada preset se calcula en `dateRange.ts` con `Date.UTC` sobre los getters locales (mismo patrón ya usado para arreglar el bug de fechas del backend en `reports-pnl`/`reports-sales` — mezclar construcción local con lectura UTC corre el corte unas horas) para no reintroducir ese mismo bug del lado del frontend. El filtro por defecto sigue siendo el mes en curso, tipear las fechas a mano sigue andando igual. Probado en Chrome en las dos pestañas (Resultados y Ventas, cada una con su propio estado): los 4 rangos calculan las fechas correctas y disparan el refetch. `nx lint,test,build` de `web` 100% verde.
15. **AFIP padrón (búsqueda por CUIT) sin probar de punta a punta**: `RealAfipPadronService`/`AfipWsaaClient` están escritos según el protocolo documentado de WSAA + `ws_sr_padron_a13`, pero no hay un certificado AFIP real en ningún entorno de esta máquina todavía — sólo se probó el camino de "CUIT inválido" (validación local) y "AFIP no configurado" (stub). Falta: dar de alta un certificado en el Administrador de Relaciones de Clave Fiscal (autorizado para el servicio "Padrón", homologación primero) y cargar `AFIP_CERT_PATH`/`AFIP_KEY_PATH`/`AFIP_CUIT_REPRESENTADA` en `.env` para el primer test real.
16. ~~Condición IVA / domicilio fiscal que devuelve AFIP no se persiste en `Company`~~ — resuelto (2026-07-23). `Company` ganó `taxCondition`/`fiscalAddress` (`String?`, migración `20260730000000_company_afip_tax_condition`, puramente aditiva). `CompanyFormModal` guarda lo que devuelve "Buscar en AFIP" (antes sólo se mostraba en un mensaje efímero) y lo precarga al editar una empresa que ya lo tenía guardado; `CompanyDetailModal` los muestra en la vista de detalle. Sigue siendo texto libre no re-validado (AFIP cambia sus categorías con el tiempo, mismo criterio que el resto del padrón) y no dispara ninguna lógica todavía (elegir factura A/B/C sigue sin usar este campo — si se necesita, es un paso aparte). Probado de punta a punta: creé una empresa vía API con ambos campos, confirmé que persisten en la base, y en Chrome verifiqué que el modal de detalle y el de edición (precarga) los muestran correctamente. `nx lint,test,build` de `companies`/`database`/`web` 100% verde.
17. ~~**Análisis de referencia contra un competidor**~~ (sesión 2026-07-22, un sistema contable competidor que usa el usuario): documento comparando navegación, densidad, formularios y vocabulario contra PLEXO. Las 5 recomendaciones están implementadas: autocompletar por CUIT (punto 15), checklist de "primeros pasos", panel de detalle de factura, tabs Resumen/Gestión en Facturación y Cuentas a Cobrar, y densidad compacta opcional (ver tabla de arriba, commits `bc9f2c6`/`8e58f7b`/`26f37e1`). Sin pendientes de ese análisis.

## Para retomar

```bash
cd erp_plexo
git pull
npm install
npx prisma generate
# Postgres: en esta máquina ya está configurado (ver arriba). En otra, levantar según corresponda.
npx nx serve api            # http://localhost:3000/api  (WebSocket en :3001)
cd apps/web && npx next dev -p 4200   # http://localhost:4200
```

Login de prueba (esta PC): `tenantId=79dcca57-3830-4f17-af13-c000c0c0d0df`, `owner@demo.plexo` / `changeme123`. (El `tenantId=f307123c-...` anotado antes acá corresponde al seed de la otra máquina donde se hizo el tablero — cada máquina tiene su propia base local, el tenantId no viaja con git.)
